---
id: strings_chto_znachit_immutabelnost_strok
deck: Go
tags:
  - strings
---

# Front

Что значит иммутабельность строк в Go?

# Back

Нельзя изменить отдельный байт строки: `s[0] = 'X'` — ошибка компиляции.

Любая «модификация» создаёт новую строку.

Это позволяет безопасно передавать строки между горутинами без копирования и использовать их как ключи `map`.
